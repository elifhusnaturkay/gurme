import 'package:flutter/material.dart';

class FilteredSearchScreen extends StatelessWidget {
  const FilteredSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
